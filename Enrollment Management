// enrollmentManager.js
let enrolledStudents = [];

function enrollStudent(studentName, className) {
  enrolledStudents.push({ student: studentName, class: className });
}

function viewEnrolledStudents() {
  return enrolledStudents;
}

module.exports = { enrollStudent, viewEnrolledStudents };
